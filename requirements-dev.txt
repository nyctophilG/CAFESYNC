-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
httpx>=0.27

# Playwright for end-to-end browser tests.
# After install, run `playwright install chromium` once to download the browser.
pytest-playwright>=0.5
playwright>=1.40
